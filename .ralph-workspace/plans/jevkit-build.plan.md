---
name: jevkit-build
overview: "Build jevkit: a Go single-binary CLI + MCP server + agent hooks/plugins for TypeSafe AI's Jev (closed-set SystemOne API), with token usage tracking, ralph-style flexible API key storage, and automated releases + changelog on merge to main. Port from ralph at /Users/joshuajancula/Documents/projects/ralph (read-only reference). Design: docs/DESIGN.md."
mode: standard
instructions: "Execute one TODO at a time. Read docs/DESIGN.md first for the design and the ralph reference paths (all under /Users/joshuajancula/Documents/projects/ralph; treat ralph as read-only and never modify it). After each TODO, run the verification steps listed (build, test, lint, or equivalents; use `make` targets once they exist); keep fixing failures until they pass, and report the result with `TODO_VERIFICATION: PASS` or `TODO_VERIFICATION: FAIL: <reason>` as appropriate. When the TODO is done, set `status: completed` rather than marking `[x]`. Testing policy: unit tests only (Go table tests with fakes/fixtures, in-process handlers, temp dirs); do NOT add integration, end-to-end or live per-runtime tests, and do not launch real agents. Use Conventional Commits. Never hardcode or log an API key. Targets: Claude Code, OpenCode, Cursor, Antigravity (Codex optional). Copilot and Grok are out of scope."

todos:
  # ---- Phase 0: scaffolding and spike ----
  - id: repo-scaffold
    content: "Initialize the git repo (branch main) in jevkit/, Go module (github.com/<owner>/jevkit; use a placeholder owner until confirmed), cmd/jevkit/main.go with a `version` command, internal/ package skeleton (jev, redact, keystore, breaker, registry, usage, compact, mcp, exec, agents), .gitignore (include .ralph-workspace/), LICENSE placeholder, README stub, and a Makefile with targets build, test, lint, snapshot."
    verification: "Run `make build` and `./bin/jevkit version`; run `make test` and `make lint` (golangci-lint config included) and confirm they pass. Report TODO_VERIFICATION: PASS or FAIL: <reason>."
    status: completed
  - id: devcontainer-docker
    content: "Add contributor tooling: .devcontainer/devcontainer.json and a multi-stage Dockerfile (Go toolchain, golangci-lint, goreleaser, git-cliff) so `docker build` + `docker run ... make test` reproduces CI. Docker is for contributors only, never runtime. Document in CONTRIBUTING.md."
    verification: "Run `docker build -t jevkit-dev .` and `docker run --rm jevkit-dev make test` and confirm both pass. Report TODO_VERIFICATION."
    status: completed
  - id: spike-hook-payloads
    content: "Phase 0 spike: from ralph, capture the real hook payload/response shapes for Claude Code (PostToolUse:Bash, updatedToolOutput), Cursor, Antigravity, OpenCode (plugin lifecycle tool.execute.before/after) and Codex. Read ralph bundle/.claude/hooks/compact-bash-output.sh, bundle/.cursor/hooks/*.sh and hooks.json, bundle/.agents/hooks/*.sh and hooks.json, bundle/.codex/hooks/*.sh, bundle/.opencode/plugins/ralph-runtime-hooks.ts, bundle/.ralph/docs/HOOKS.md and TOOLING.md, bash-lib/native-hook/. Write docs/AGENT-CAPABILITIES.md (per agent: events, config file/location, payload fields incl. whether a real exit code is present, output-replacement vs PreToolUse-rewrite-to-`jevkit exec`) and save sanitized fixture payloads under testdata/hooks/<agent>/."
    verification: "Confirm docs/AGENT-CAPABILITIES.md covers all four target agents plus Codex, every fixture is valid JSON (`jq empty testdata/hooks/*/*.json`), and no secrets or absolute home paths appear in fixtures (grep). Report TODO_VERIFICATION."
    status: completed
  - id: port-fixtures-registry
    content: "Copy jev fixtures and the question registry from ralph into jevkit: tests/fixtures/jev/*.json -> testdata/jev/, bundle/.ralph/jev/questions.registry.json -> internal/registry/questions.registry.json, schemas/jev-question-set.schema.json -> internal/registry/. Rename Ralph-flavored identifiers (RALPH_JEV_* -> JEVKIT_*, ralph.jev -> jevkit) in any copied content; do not copy behavior that depends on ralph run-plan or graph machinery."
    verification: "Confirm `jq empty` passes on all copied JSON and `grep -ri ralph testdata internal/registry` returns nothing except intentional provenance notes. Report TODO_VERIFICATION."
    status: completed

  # ---- Phase 1: core ----
  - id: jev-types-client
    content: "Implement internal/jev: typed request/response models (question types noul, choice, score as a sealed/generic type set; strict JSON decoding rejecting a response without `answers`), a Client that POSTs to https://api.typesafe.ai/v1/systemone (override via JEVKIT_ENDPOINT; model alias jev-latest via JEVKIT_MODEL; timeout JEVKIT_TIMEOUT_MS default 4000) with Authorization: Bearer, strips Ralph-internal `questionSetId` before send, enforces the 32000-token (4 bytes/token) size budget and 255-option choice cap. Status handling: 401/422 = config error, no retry, open breaker; 429/529 retry with exponential backoff up to JEVKIT_MAX_RETRIES (default 2); other non-2xx fail. Error taxonomy mirrors ralph exit codes (declined=1, transport/protocol=2, input rejected pre-network=3). Support a fixture transport (JEVKIT_TRANSPORT=fixture, JEVKIT_FIXTURE_DIR). Ref: ralph python/jev_client.py, bash-lib/jev/jev-client.sh."
    verification: "Write table-driven tests with an httptest fake server and the ported fixtures covering success, 401/422 (no retry), 429/529 retry, malformed body, oversize input, and the key never appearing in logs or errors. Run `go test ./internal/jev/... -race` and `make lint`. Report TODO_VERIFICATION."
    status: completed
  - id: redaction
    content: "Implement the built-in redaction engine in internal/redact, fail-closed, run before ANY send. Every rule has a stable id (e.g. builtin.aws-access-key) and a class: HARD (cannot be disabled or allowlisted by anyone: the live API key value, PEM/private-key blocks, known token formats sk-/AKIA/ghp_/xox/etc., Authorization/Bearer headers, credential assignments like password=/secret=/token=) or SOFT (tunable: $HOME/user paths, high-entropy strings, emails, IPs, UPPER=value env dumps). Redaction is line-preserving: never add, drop or merge newlines (multi-line matches are replaced per line) so L000.. line tags stay aligned. After redaction run a verification pass that no known secret value (live key, env-var values) survives; if redaction errors or verification fails, reject the input pre-network (code 3). All patterns use Go RE2 (no ReDoS). Expose a Redactor with Apply(text) (Result{Text, Hits[]{RuleID, Count}}) so callers and the audit log see which rules fired but never the matched content. Ref: ralph bash-lib/jev/jev-redact.sh and python/jev_redact.py, tests/bats/jev/jev-redact.bats."
    verification: "Port the cases from jev-redact.bats as Go table tests; add fuzz/property tests that redacted output never contains a seeded secret and that line count is preserved; test that a failing rule or verification failure yields the reject error. Run `go test ./internal/redact/... -race`. Report TODO_VERIFICATION: PASS or FAIL: <reason>."
    status: completed
  - id: redact-config
    content: "Add user-editable redaction config: a YAML schema (embedded JSON Schema, versioned with `version: 1`) and loader in internal/redact/config. Layers, lowest to highest precedence: (1) embedded built-ins; (2) USER config at <config_dir>/redact.yaml (trusted); (3) PROJECT config at <workspace>/.jevkit/redact.yaml (UNTRUSTED, may come from a cloned repo, so it is additive-only: it may add rules, literals, env names and never_send entries but can NEVER disable/loosen a rule, add an allowlist entry or lower a threshold; any such key is a load error naming the file and key). Supported keys: `rules:` (custom regex rules with id, pattern, replacement label, optional flags), `literals:` (exact strings/secrets to always redact), `env_values:` (env var names whose current values are always redacted, plus glob names such as *_TOKEN), `allowlist:` (USER only; regex or literal exemptions that apply to SOFT rules only, never HARD), `disable:` (USER only; soft rule ids), `tuning:` (entropy threshold, minimum token length, path handling), `never_send:` (command/path/glob patterns whose output is never sent to Jev at all, e.g. `cat .env*`, `*/secrets/*`, `kubectl get secret*`; matched before any network use), and `placeholder:` (`label` => [REDACTED:rule-id], or `stable` => [REDACTED:rule-id:abcd] using an HMAC with a per-install random salt stored 0600 so identical secrets map to the same token without being reversible). Unknown keys, invalid regexes, oversize patterns or a config that fails schema validation fail CLOSED: nothing is sent (code 3) and hooks pass output through unchanged, with a clear error. Files must be 0600/not world-writable for USER config (warn and refuse otherwise)."
    verification: "Table tests for layering and precedence; that project config cannot disable/allowlist/loosen anything (each attempt errors with file+key); that HARD rules ignore allowlist/disable; invalid regex, unknown key, wrong version and bad file permissions all fail closed; stable placeholders are deterministic per salt and differ across salts. Run `go test ./internal/redact/... -race`. Report TODO_VERIFICATION: PASS or FAIL: <reason>."
    status: completed
  - id: redact-tuning
    content: "Implement the tunable detectors and controls behind the config: high-entropy token detection (Shannon entropy over candidate tokens with configurable threshold and min length, defaults tuned to avoid flagging hashes in git output too aggressively but redacting when in doubt in strict mode), env-value redaction (auto-redact values of env vars whose names match *KEY*, *TOKEN*, *SECRET*, *PASSWORD*, *CREDENTIAL* plus user `env_values`), custom regex/literal rules with placeholders, allowlist handling for SOFT rules only, and `never_send` matching that short-circuits before redaction/network. Add a global `mode:` of `standard` or `strict` (strict = redact all SOFT rules and lower entropy threshold, cannot be loosened by project config)."
    verification: "Table tests per detector including false-positive cases the allowlist fixes, strict vs standard behavior, env-value redaction with a seeded environment, never_send short-circuit (no jev client call is made, verified with a fake client that fails the test if invoked). Run `go test ./internal/redact/... -race`. Report TODO_VERIFICATION: PASS or FAIL: <reason>."
    status: completed
  - id: redact-cli
    content: "Add the `jevkit redact` command group so users can inspect and fine-tune redaction: `redact init` (writes a heavily commented starter redact.yaml at 0600 in the user config dir, or `--project` for .jevkit/redact.yaml), `redact list` (all rules with id, class HARD/SOFT, source layer, enabled/disabled), `redact explain <rule-id>` (pattern, class, how to tune), `redact test [file|-]` (runs the full engine on stdin/file and prints the redacted text, `--diff` for a unified diff, and a per-rule hit table; never sends anything to the network), `redact add --pattern|--literal|--env|--never-send <value>` (edits the YAML in place preserving comments via yaml.v3 Node API, validates before writing, atomic write), and `redact check` (validates config, lints for duplicate/shadowed/never-matching rules and over-broad patterns, and runs any `tests:` cases embedded in the YAML: each case has `input` and `must_not_contain` / `must_contain` so users can regression-test their own rules). `jevkit doctor` reports redaction config validity, active layers and mode."
    verification: "Unit-test each subcommand in-process with injected buffers and temp config dirs: init creates 0600 file that itself passes `check`; add preserves existing comments and rejects invalid input without modifying the file; test/--diff output is correct and makes no network call; check catches a deliberately broken and a shadowed rule and runs embedded tests cases; doctor reports config errors. Run `go test ./cmd/... ./internal/redact/... -race`. Report TODO_VERIFICATION: PASS or FAIL: <reason>."
    status: completed
  - id: redact-audit-review
    content: "Add transparency for what leaves the machine: (1) a local audit log at <state>/jevkit/redaction-audit.jsonl recording per send: timestamp, agent, question set, bytes before/after, and per-rule hit counts (rule ids and counts only, NEVER matched content), with `jevkit redact audit [--since] [--format json]` to summarise; (2) an opt-in review mode (`redact.review: true` or JEVKIT_REDACT_REVIEW=1) that stores the exact already-redacted payload of the last N sends (default 20, TTL default 24h, file mode 0600, purged automatically) so `jevkit redact last [n]` shows precisely what was sent to TypeSafe AI; off by default because stored payloads are themselves sensitive; (3) `redact.confirm: true` optional mode for `jevkit key test`/MCP jev_ask style calls requiring an explicit interactive confirm showing the redacted payload before sending (not used in hooks, which must stay non-interactive)."
    verification: "Unit tests: audit lines contain rule ids/counts and never contain a seeded secret or matched text; review store honors N, TTL purge and 0600 perms and is empty when disabled; audit summary aggregation; confirm mode blocks until the injected prompter answers yes and aborts on no. Run `go test ./internal/redact/... ./cmd/... -race`. Report TODO_VERIFICATION: PASS or FAIL: <reason>."
    status: completed
  - id: redact-docs
    content: "Write docs/REDACTION.md: the threat model (what is sent to api.typesafe.ai and what never is), the layered config model and why project config is additive-only, HARD vs SOFT rules, every config key with examples (custom rule, literal, env_values, allowlist for a false positive, never_send, strict mode, stable placeholders), the `redact test/check/audit/last` workflow for tuning, and a security checklist for users handling regulated data. Link it from the README."
    verification: "Add a Go test that extracts every yaml code fence in docs/REDACTION.md and asserts each parses and passes schema validation, and that every documented `jevkit redact` subcommand exists in the cobra tree. Run `go test ./internal/redact/... ./cmd/...`. Report TODO_VERIFICATION: PASS or FAIL: <reason>."
    status: completed
  - id: keystore
    content: "Implement internal/keystore with the 5-source resolution chain, first hit wins, exposing the winning source (env|env-file|command|keychain|file|none): (1) JEVKIT_API_KEY / TYPESAFE_API_KEY env; (2) .env in workspace root, parsing only the key line (never sourced/eval'd; handle `export ` prefix and quotes; disable with JEVKIT_ENV_FILE=0); (3) stored command run with context timeout (no gtimeout dependency), stdout is the key; (4) OS keychain via zalando/go-keyring (service jevkit, account TYPESAFE_API_KEY), silently skipped if unavailable; (5) 0600 plaintext file in config dir as last resort with warning at set time. Config dir: $JEVKIT_CONFIG_HOME, else XDG config, else platform default. Backend selection in jev-credentials.json (0600), command backend takes precedence. Keys never stored under workspace state dirs. Ref: ralph bash-lib/jev/jev-key-store.sh and tests/bats/jev/jev-key-store.bats."
    verification: "Port jev-key-store.bats scenarios as Go tests using an in-memory fake keyring backend and temp dirs (precedence order, .env parsing edge cases, command timeout/nonzero exit, file perms 0600, no key in argv or logs). Run `go test ./internal/keystore/... -race`. Report TODO_VERIFICATION."
    status: completed
  - id: breaker-usage
    content: "Implement internal/breaker (circuit breaker state file, opens immediately on 401/422, shared safely across concurrent processes) and internal/usage: append one JSONL record per successful call to <state>/jevkit/usage.jsonl using file locking (fields: timestamp, model resolved, questionSetId, input_tokens, output_tokens, usageSource measured|unavailable, transport https|fixture, agent, planKey/session). Failed and retried attempts are not recorded. Aggregator supports filters and text/json output with estimated cost (JEVKIT_INPUT_USD_PER_MTOK default 0.042, output default 0). Ref: ralph python/jev_usage.py and tests/bats/jev/jev-usage.bats."
    verification: "Concurrency test: N goroutines/processes append simultaneously and every line stays valid JSON (`go test -race`); port jev-usage.bats aggregation cases. Run `go test ./internal/breaker/... ./internal/usage/... -race`. Report TODO_VERIFICATION."
    status: completed
  - id: registry-policy
    content: "Implement internal/registry loader (go:embed the question registry, validate against the JSON schema at startup/test) and the decision policy: confidence >= actThreshold => act; between escalate and act => gather; below escalateThreshold => fallback; JEVKIT_SHADOW=1 records the would-have decision but returns fallback. Thresholds come only from the registry. Decisions logged to decisions.jsonl. Ref: ralph bash-lib/jev/jev-policy.sh, python/jev_shadow_report.py, tests/python/test_jev_question_registry.py."
    verification: "Table tests for threshold boundaries, shadow mode, and schema validation of the embedded registry. Run `go test ./internal/registry/... -race`. Report TODO_VERIFICATION."
    status: completed
  - id: cli-core
    content: "Build the CLI with cobra: `jevkit key set|status|clear|test` (set reads with echo off on a TTY or from stdin; refuses key as argv; --command stores a credential command; `key test` is the only command that makes a live call), `jevkit usage [--format text|json] [--include-fixture]`, `jevkit doctor` (key source, endpoint reachability, breaker state, installed agents), `jevkit version`, and `jevkit migrate-from-ralph` (one-time copy of ralph's key/config from the ralph.jev keychain entry and ~/.config/ralph/jev-credentials.json, never printing the key). Ref: ralph bundle/.ralph/jev.sh."
    verification: "Unit-test each command handler in-process (cobra command with injected fake keystore, fake jev client and buffers): `key set` rejects an argv key, `doctor` and all output never contain the key, `usage` formats text/json, `migrate-from-ralph` reads fixture inputs. Run `go test ./cmd/... ./internal/... -race` and `make lint`. Report TODO_VERIFICATION: PASS or FAIL: <reason>."
    status: completed

  # ---- Phase 2: MCP ----
  - id: mcp-server
    content: "Implement internal/mcp as a stdio JSON-RPC server (`jevkit mcp start`) using the official Go MCP SDK (verify the current module path and pin it). Tools: jev_classify_request (graph.router-confidence), jev_classify_failure (graph.failure-class), jev_rank_relevance (compaction.line-relevance), and jev_ask (raw, unversioned). Curated tools reject unknown keys (-32602) and require `options` (1-255 non-empty strings) where a choice question has no fixed criteria; successful results carry content text plus structuredContent {answer, decision, questionSetId, registryVersion}. Jev unavailable returns a successful envelope with available:false, isError:false. Never emit a null nextCursor in tools/list. All logging to stderr. Resolve the key inside the server; generated client configs carry no key. Ref: ralph bundle/.ralph/jev-mcp-server.sh, tests/bats/jev/jev-mcp.bats."
    verification: "Port jev-mcp.bats as Go unit tests driving the server in-process over io.Pipe (initialize, tools/list without nextCursor, each tool, bad args, unavailable case). Run `go test ./internal/mcp/... -race`. Report TODO_VERIFICATION: PASS or FAIL: <reason>."
    status: completed
  - id: mcp-config-cmd
    content: "Add `jevkit mcp status|config` (print or --merge the MCP server entry into a client config such as .mcp.json without embedding any key, preflight check failures are non-fatal with a reason)."
    verification: "Test that config output has no secret, --merge is idempotent, and status reports key source. Run `go test ./internal/mcp/... ./cmd/...`. Report TODO_VERIFICATION."
    status: completed

  # ---- Phase 3: compaction pipeline + Claude hook ----
  - id: compact-deterministic
    content: "Implement internal/compact deterministic layer ported from ralph python/shell-output-compact.py and bash-lib/compactors.sh: family classification with hard passthrough for source-output families (git diff/show/log, grep, rg, find, ls, tree), collapse of repeated line runs, byte budget assembly. Only the pieces needed for the jev tier's fallback path; do not port unrelated ralph families unless trivial."
    verification: "Port golden tests from ralph tests/bats/compactors.bats and tests/fixtures/compactors for the ported families; run `go test ./internal/compact/... -race`. Report TODO_VERIFICATION."
    status: completed
  - id: compact-jev-ranked
    content: "Implement the jev ranked-line tier in internal/compact (ralph _compact_jev_ranked ~L808-1100, dispatch when no family matched ~L329-341): tag surviving lines L000.., cap windows at 255 lines per request, ask compaction.line-relevance (relevant_lines choice, has_failure noul), assemble head + ranked middle + tail under the byte budget, treat jev output as untrusted (safety gate + preserve-line round trip on failures), any abort returns the deterministic result unchanged. Gate: JEVKIT_COMPACT=1 (or per-agent config); size threshold before calling jev; SHADOW mode by default records would-have savings to jev-compact.jsonl without altering output. Feed real exit code for failure-aware ranking."
    verification: "Port tests/bats/compactor/jev-ranked.bats cases as golden tests with the fixture transport (ranked selection, safety gate abort, preserve-line, shadow mode leaves output unchanged, passthrough families never sent to jev). Run `go test ./internal/compact/... -race`. Report TODO_VERIFICATION."
    status: completed
  - id: exec-wrapper
    content: "Implement `jevkit exec -- <cmd>` (internal/exec): run the command, capture stdout/stderr and the REAL exit code, run through the compaction pipeline, print result and exit with the original code. Must be cross-platform (no bash dependency), preserve signal exit semantics, and fail open (on any internal error print original output). This is the primary compaction path for Cursor, Codex, Antigravity and OpenCode. Ref: ralph bash-lib/native-hook/native-shell-wrapper.sh (ralph_native_shell_compact_pipeline_json)."
    verification: "Tests: exit code passthrough, large output compaction, jev-down fail-open, quoting/argv fidelity on unix and windows (CI matrix). Run `go test ./internal/exec/... -race`. Report TODO_VERIFICATION."
    status: completed
  - id: hook-framework
    content: "Implement the `jevkit hook <agent> <event>` framework in internal/agents: read stdin JSON, dispatch to an Agent adapter interface (Name, Capabilities, HandlePreTool, HandlePostTool, Stop/telemetry as applicable, Install/Uninstall config), with panic recovery, a hard timeout, fail-open passthrough (always exit 0 with a valid empty/allow response unless a deliberate deny), and a protocol-version handshake that fails open on mismatch. Add telemetry that records hook invocations to usage tracking."
    verification: "Unit tests with the Phase 0 fixtures: garbage stdin, panic in adapter, timeout, and version mismatch all yield a safe response and exit 0. Run `go test ./internal/agents/... -race`. Report TODO_VERIFICATION."
    status: completed
  - id: agent-claude
    content: "Implement the Claude Code adapter: PostToolUse:Bash hook returning hookSpecificOutput.updatedToolOutput with compacted output (only above the size threshold, only when jev compaction enabled; shadow mode passes through), plus installer that merges hooks into .claude/settings.json (project) or ~/.claude/settings.json (user) idempotently with markers, --dry-run and uninstall. Ref: ralph bundle/.claude/hooks/compact-bash-output.sh, bundle/.ralph/plugin-inputs/templates/claude/hooks.json. Confirm from the spike whether the payload carries a real exit code."
    verification: "Unit tests only: fixture-driven hook handler tests (compacted output returned above threshold, passthrough below threshold and in shadow mode) and installer tests on temp dirs (apply twice = one entry; uninstall restores the original bytes). Run `go test ./internal/agents/... -race`. Report TODO_VERIFICATION: PASS or FAIL: <reason>."
    status: completed

  # ---- Phase 4: remaining agents + plugins ----
  - id: agent-cursor
    content: "Implement the Cursor adapter: preToolUse (Shell) rewrite to `jevkit exec -- <cmd>` via updated_input, postToolUse for native read/grep/glob and MCP:* results where output replacement is supported, telemetry hooks, and installer for .cursor/hooks.json (version 1, camelCase events). Ref: ralph bundle/.cursor/hooks.json and hooks/*.sh."
    verification: "Unit tests only: fixture-driven hook handler tests (Shell command rewritten to `jevkit exec`, MCP/native result handling) and installer tests on temp dirs (idempotent, uninstall restores original). Run `go test ./internal/agents/... -race`. Report TODO_VERIFICATION: PASS or FAIL: <reason>."
    status: completed
  - id: agent-antigravity
    content: "Implement the Antigravity adapter: pre-tool shell rewrite to `jevkit exec`, allow/decision responses, telemetry, and installer for .agents/hooks.json. Ref: ralph bundle/.agents/hooks.json and hooks/pre-tool-shell-policy.sh."
    verification: "Unit tests only: fixture-driven hook handler tests (shell rewrite, allow response) and installer tests on temp dirs (idempotent, uninstall restores original). Run `go test ./internal/agents/... -race`. Report TODO_VERIFICATION: PASS or FAIL: <reason>."
    status: completed
  - id: agent-opencode
    content: "Implement the OpenCode integration: a small TypeScript plugin (tool.execute.before rewrites shell commands to `jevkit exec -- ...`, tool.execute.after for telemetry) that shells out to the jevkit binary, plus installer placing it under .opencode/plugins/. Verify per ralph's SPIKE-output-mutation.md whether model-visible output mutation works; rely on the wrapper rewrite either way. Ref: ralph bundle/.opencode/plugins/ralph-runtime-hooks.ts."
    verification: "Unit tests only: Go-side tests for the installer and for the rewrite/telemetry handler with fixture payloads, and `tsc --noEmit` plus a plugin unit test (mocked shell exec) for the TypeScript. Report TODO_VERIFICATION: PASS or FAIL: <reason>."
    status: completed
  - id: agent-codex
    content: "Optional (ralph already supports it): Codex adapter with pre-tool bash rewrite to `jevkit exec`, telemetry, and installer for .codex/hooks.json. Ref: ralph bundle/.codex/hooks/*.sh and hooks.json. Skip if scope needs trimming and mark status: cancelled with a reason."
    verification: "Fixture-driven hook tests and installer idempotency tests. Report TODO_VERIFICATION."
    status: completed
  - id: install-command
    content: "Implement `jevkit install <agent|all>` and `jevkit uninstall <agent|all>` over the adapters: detect installed agents, choose project vs user scope, idempotent marker-based edits, --dry-run diff preview, backups, and registration of the MCP server with each agent. `jevkit doctor` reports per-agent install state."
    verification: "Unit tests on temp HOME/project dirs with fake agent adapters: detection, install is idempotent, --dry-run writes nothing, backups created, uninstall restores original files byte-for-byte, `doctor` reports install state. Run `go test ./internal/agents/... ./cmd/... -race`. Report TODO_VERIFICATION: PASS or FAIL: <reason>."
    status: completed
  - id: plugin-packages
    content: "Generate distributable plugin packages so users can connect with one command per agent: Claude Code plugin (plugin.json, .mcp.json, hooks.json calling `jevkit`, marketplace manifest), Cursor, Antigravity and OpenCode equivalents. Each plugin checks that `jevkit` is on PATH and prints the install command (or bootstraps a pinned version) if missing. Build them from templates in plugins/ via `make plugins`, committed or release-attached. Ref: ralph bundle/.ralph/plugin-inputs/ and bash-lib/plugin/plugin-adapter-*.sh."
    verification: "Unit tests: generated manifests for each host are valid JSON matching a checked-in schema for that host, contain no secrets, reference `jevkit` correctly, and the plugin version matches the binary version constant. Run `go test ./internal/plugins/...` and `make plugins`. Report TODO_VERIFICATION: PASS or FAIL: <reason>."
    status: completed
  - id: agent-capability-docs
    content: "Write user docs: README (what jev is and is not, quickstart, per-agent capability matrix with which mechanism each agent uses, key storage options and precedence, usage tracking, shadow mode and how to enable compaction, security/redaction notes), docs/AGENTS.md, and docs/KEYS.md."
    verification: "Run a markdown link check and confirm every command shown in the README and docs exists in `jevkit --help` output (test that parses the docs code fences against the cobra command tree). Report TODO_VERIFICATION: PASS or FAIL: <reason>."
    status: completed

  # ---- Phase 4.25: runtime integration and compaction architecture correction ----
  - id: runtime-capability-audit
    content: "Replace assumptions about agent hooks with an evidence-backed capability matrix for the exact supported runtime versions. For each runtime, record: whether a post-tool event includes the completed output and an authoritative exit/error status; whether it can replace the model-visible result (and the exact result schema); whether it can only append/inject context; whether it can safely run asynchronous telemetry; and its plugin/config installation mechanism. Use official runtime documentation plus sanitized fixture payloads and compile/unit-level adapter tests; do not infer result replacement from a pre-tool rewrite. Establish the policy matrix: Claude Code may use native PostToolUse replacement only after schema validation; Codex may use PostToolUse feedback/additional context but must not claim it can replace arbitrary original output; Antigravity PostToolUse is telemetry-only because its response is empty; OpenCode and Cursor remain telemetry-only unless their currently pinned APIs prove model-visible replacement with a shape-preserving test. Document runtime versions, evidence links, and feature gates in docs/AGENTS.md."
    verification: "Add fixture-driven tests proving each adapter only selects a capability it has evidence for; validate the generated hook/plugin configuration against its checked-in schema; assert docs/AGENTS.md has a row for Claude Code, Codex, Cursor, Antigravity, and OpenCode that names result replacement, context injection, or telemetry-only explicitly. Run `go test ./internal/agents/... ./internal/plugins/... -race` and the relevant TypeScript type check. Report TODO_VERIFICATION: PASS or FAIL: <reason>."
    status: completed
  - id: runtime-integration-surface
    content: "Make the public CLI match the product architecture: public commands are user tasks (`install`, `uninstall`, `mcp`, `ask`, key/redaction/usage/doctor/version), not hook protocol plumbing. Remove public root `hook` and `exec` commands, their help entries, and all user-facing documentation/examples. Do not retain command-rewriting as the normal compaction path. If a runtime needs a binary entrypoint for an installed package, expose one private, hidden runtime dispatcher with a deliberately non-user-facing contract; only generated/installed integration assets may invoke it, and it must be versioned, fail open, and absent from normal `--help`/shell completion. Redesign `jevkit install <agent|all>` around explicit components: `--components plugin,mcp,hooks` (default `plugin`, which installs that runtime's supported bundle), allowing `mcp` or `hooks` alone and rejecting unsupported combinations based on the capability audit. Installation, dry-run previews, upgrades, and uninstall must be component-aware and restore each config byte-exactly without touching components the user did not select."
    verification: "Cobra tree tests assert root help and completions expose neither `hook` nor `exec`; integration-asset tests prove the private dispatcher works only with its versioned envelope and fails open; install tests cover plugin default, MCP-only, hooks-only, unsupported component rejection, idempotency, dry-run writes nothing, selective uninstall, and byte-exact restoration. Test generated packages contain no public hook/exec command references. Run `go test ./cmd/... ./internal/agents/... ./internal/plugins/... -race`, `make plugins`, and `make lint`. Report TODO_VERIFICATION: PASS or FAIL: <reason>."
    status: completed
  - id: compaction-disposition-policy
    content: "Replace the current always-rank-large-output behavior with a runtime-neutral, conservative disposition policy. Jev is a closed-set classifier, not a text generator: it may decide and rank existing evidence, but it must never invent a free-form summary. First classify output locally as immutable/source data (binary, diffs/patches, source/code, structured documents, short output) versus diagnostic tool output (build, test, lint, compiler, runtime/log output). Immutable/source data always remains full and is never sent. For eligible diagnostic output over a configurable threshold, send already-redacted, bounded evidence plus authoritative exit status when available to a versioned registry question set that returns both `outcome` (success, failure, unknown) and `disposition` (full, deterministic-compact, ranked-diagnostic). The local policy must preserve full output on unknown/low confidence, invalid/slow/unavailable Jev, source-data detection, or any runtime that cannot replace results. On confirmed failures, retain the error line(s), command/exit context, causal neighbours, and head/tail; on confirmed success, deterministic repetition collapse is allowed only when the selected runtime can replace the result. Keep strict size reduction and preserve-line gates; shadow mode records the proposed disposition, reason, byte savings, confidence, and runtime without changing output."
    verification: "Golden/table tests cover every disposition and fallback: source/binary/structured output never sent; short output untouched; authoritative non-zero exit produces failure context; no-exit runtimes remain unknown unless Jev classifies; low confidence, timeout, malformed response, redaction failure, and unsupported replacement all preserve original output; selected diagnostics preserve required evidence and are strictly smaller. Verify shadow records contain no payload or secret. Run `go test ./internal/compact/... ./internal/registry/... -race` and `make lint`. Report TODO_VERIFICATION: PASS or FAIL: <reason>."
    status: completed
  - id: runtime-compaction-rollout
    content: "Implement adapters from the capability matrix rather than forcing one wrapper pattern on every runtime. Claude Code: use native PostToolUse/PostToolUseFailure result replacement only for validated result shapes and the disposition policy; otherwise leave original output. Codex: use PostToolUse only for bounded, non-authoritative feedback/telemetry or model-context guidance when supported; never suppress or claim to compact the original result unless the audited API proves a safe replacement path. Cursor/OpenCode: enable output replacement only if the audited pinned integration API provides a fixture-tested shape; otherwise keep hooks telemetry-only and rely on MCP tools. Antigravity: keep PostToolUse telemetry-only and do not inject transcript-derived output. All runtimes install MCP independently of hooks; hooks are opt-in, non-blocking, respect compact/shadow gates, and never execute the agent's shell command themselves. Update plugin templates, generated packages, docs, diagnostics, and migration-safe installer behavior accordingly."
    verification: "Fixture-driven adapter/package tests verify each runtime's selected behavior, no adapter rewrites a command through an execution wrapper, unsupported runtimes leave model-visible output unchanged, supported replacement is shape-valid, all telemetry redacts before persistence, and MCP-only installs register no hook. Run `go test ./internal/agents/... ./internal/compact/... ./internal/plugins/... -race`, the plugin TypeScript tests/type checks, `make plugins`, and `make lint test`. Report TODO_VERIFICATION: PASS or FAIL: <reason>."
    status: completed
  - id: compaction-policy-configuration
    content: "Add a safe, user-editable compaction policy file. Support a trusted user policy and additive-only project policy with reviewed command/output matchers, explicit actions (never-compact, deterministic-only, eligible), threshold overrides within safe bounds, and explanatory `jevkit compact explain`/`validate` commands. Policies must be declarative only: no shell, templates, arbitrary code, output replacement text, or ability to override hard binary/source-data protections, redaction, confidence gates, size-reduction checks, or runtime replacement capability. Expose the matched rule/reason in shadow metadata and doctor output without recording payload text. Document examples for protecting custom commands, treating known build/test commands as eligible, and testing policy against fixture output."
    verification: "Table tests cover precedence, malformed/unsafe config rejection, project additive-only limits, hard-protection non-overridable behavior, matcher/action selection, threshold bounds, explain output, and no payload logging. Run `go test ./internal/compact/... ./cmd/... -race` and `make lint`. Report TODO_VERIFICATION: PASS or FAIL: <reason>."
    status: completed

  # ---- Phase 4.5: rich Jev questions for people and coding agents ----
  - id: rich-question-model
    content: "Make internal/jev faithfully represent the documented SystemOne question protocol before expanding callers. State and instructions must accept a JSON string, object, or array; Choice criteria must be a required map whose values may be string, object, array, or null; Noul criteria must be an optional true/false object with the same value types; Score criteria must be a required ordered array of 2-10 string/object/array values. Preserve plain strings as the ergonomic default. Validate the complete request locally before transport: non-empty question ids, no unknown question type, Choice 1-255 distinct criteria keys, Score 2-10 levels, valid JSON types, no duplicate/conflicting shorthand and raw criteria. Correct request serialization so Choice always uses the documented `criteria` member (never `options`), and expand response models/formatters to retain Score legend and probability distribution as well as Choice probabilities/confidence. Redaction must be JSON-aware: recursively redact values and semantic keys while preserving valid JSON; detect redaction-induced key collisions and fail closed rather than silently changing a question. Keep the existing byte budget, never log raw request content or keys, and retain the fixture transport."
    verification: "Table-driven wire tests covering the documented Choice, Noul and Score examples with structured state/instructions/criteria; invalid-schema and API-limit tests; JSON-aware redaction tests proving valid JSON is retained, a seeded secret never reaches the fake transport or error/log output, and a key collision blocks the send; response round-trip tests for Choice and Score distributions/legend. Run `go test ./internal/jev/... ./internal/redact/... -race` and `make lint`. Report TODO_VERIFICATION: PASS or FAIL: <reason>."
    status: completed
  - id: ask-rich-requests
    content: "Evolve the human CLI from simple labels into a complete, safe adapter for documented Jev requests. Retain the concise typed forms (`ask noul|choice|score`) but add mutually exclusive rich inputs: `--state-json`/`--state-file`, `--instructions-json`/`--instructions-file`, and `--criteria-json`/`--criteria-file`; `--options \"pass,fail\"` remains a Choice-only shorthand that expands to null-valued criteria, while `--levels` remains the Score shorthand. Add `jevkit ask request --file <json|->` for a full API-shaped request with multiple named questions, optional model override, and stdin support; its input must be validated against the local typed schema, redacted before transport, and never echoed unredacted. Produce human-readable output for every named answer plus exact JSON with model, usage, answer type, confidence, probabilities, and Score legend. Make `--help` show a concise shorthand example and the full detailed-criteria example (including a Choice criteria object like billing/technical/sales); add README guidance on when to use each form and how shell quoting/file input works."
    verification: "In-process Cobra tests with fake clients for comma-list shorthand, detailed Choice criteria, structured state/instructions, Noul criteria, Score structured levels, a multi-question full request from a temp file and stdin, mutually exclusive/malformed input rejection, response formatting, and redaction before the fake client. Assert every `ask` help example parses and every request emitted on the wire matches the documented JSON shape. Run `go test ./cmd/jevkit/... ./internal/jev/... -race` and `make lint`. Report TODO_VERIFICATION: PASS or FAIL: <reason>."
    status: completed
  - id: mcp-rich-ask
    content: "Bring MCP `jev_ask` to the same documented protocol and safety standard as the rich CLI. Its input schema must accept structured state and a named questions map with structured instructions and documented criteria values, returning every answer with full probability/legend data and usage/model metadata. Normalize the existing raw `options` field only as a deprecated compatibility alias for null-valued Choice criteria; reject it when combined with `criteria`, never advertise it in new examples, and remove it in the next documented breaking release. Apply JSON-aware redaction and the same limits/validation before a call. Preserve agent freedom to submit arbitrary valid questions, but mark these calls `unregistered: true`, record a privacy-safe local audit entry (timestamp, caller, question ids/types, byte counts and redaction rule counts—not payload text), and do not turn arbitrary answers into automatic tool actions. Update MCP tool descriptions and generated agent configs with a copyable detailed Choice example and explicit guidance to use a file/structured arguments for large context."
    verification: "Drive the stdio MCP server in-process with fixture clients: documented detailed Choice criteria, object/array state and instructions, multi-question request, Score legend/probabilities, legacy alias normalization, alias-plus-criteria rejection, invalid structured payloads, redaction/no-secret logging, unavailable behavior, and audit metadata. Confirm tools/list and generated config describe `criteria`, not `options`. Run `go test ./internal/mcp/... ./internal/agents/... -race` and `make lint`. Report TODO_VERIFICATION: PASS or FAIL: <reason>."
    status: completed
  - id: developer-assessment-registry
    content: "Add a small, versioned set of opt-in registry-backed developer assessments for coding agents, designed as decision support rather than autonomous execution. Start with: `developer.change-risk` (Score with explicit low-to-critical rubric), `developer.failure-triage` (Choice: regression, dependency/toolchain, configuration/environment, test defect/flake, unknown; each with a detailed rubric), `developer.test-priority` (Choice: block, targeted tests, full suite, no additional tests), `developer.review-disposition` (Choice: block, needs-review, informational, no-finding), and `developer.release-readiness` (Noul with explicit true/false criteria). Define the minimum structured state schema each expects (for example diff summary, affected areas, test output, environment, and constraints), stable question-set ids/versioning, confidence thresholds, and conservative policy: low confidence always returns gather/fallback; none may modify code, run a command, merge, deploy, or expose a secret. Expose curated assessments as MCP tools or one strongly typed `jev_developer_assess` dispatcher with discoverable schemas; retain `jev_ask` for custom questions. Document calibration, shadow rollout, limitations, and how users can add a project-local untrusted additive question set without weakening built-in safety/redaction."
    verification: "Registry/schema tests validate every template and its documented example; fixture-client table tests cover each assessment's request construction, threshold boundaries, shadow/fallback behavior, unknown assessment rejection, no automatic side effects, and redaction before transport. Test MCP discovery/schema and a README example for each assessment. Run `go test ./internal/registry/... ./internal/mcp/... ./cmd/... -race` and `make lint test`. Report TODO_VERIFICATION: PASS or FAIL: <reason>."
    status: completed

  # ---- Phase 5: CI + release automation + changelog ----
  - id: ci-workflow
    content: "Add .github/workflows/ci.yml: on pull_request and push to main run devcontainer-based `make lint test` plus an OS matrix (ubuntu, macos, windows) running `go test ./... -race` including keychain-abstraction, exec-wrapper and installer tests; cache Go modules; pin actions by SHA. Add pr-title.yml enforcing Conventional Commits on PR titles (squash merge)."
    verification: "Validate workflow syntax with `actionlint` and confirm the workflows run green on a test PR (push a branch, open PR). Report TODO_VERIFICATION."
    status: completed
  - id: changelog-config
    content: "Add git-cliff config (cliff.toml) producing Keep a Changelog style CHANGELOG.md from Conventional Commits (feat/fix/perf/refactor/docs/security sections, breaking-change callouts, links to compare URLs), and seed CHANGELOG.md with an Unreleased header. Document the commit convention in CONTRIBUTING.md."
    verification: "Run `git cliff --unreleased` and `git cliff --bumped-version` locally against synthetic commits (feat -> minor, fix -> patch, `feat!` -> major, chore-only -> no bump) and confirm output. Report TODO_VERIFICATION."
    status: pending
  - id: goreleaser-config
    content: "Add .goreleaser.yaml: darwin/linux/windows x amd64/arm64, CGO_ENABLED=0, ldflags embedding version/commit/date, tar.gz (zip on windows), checksums.txt, cosign keyless signing, SBOM, GitHub artifact attestations, release notes sourced from the version's CHANGELOG section, and Homebrew tap, Scoop bucket, npm and ghcr distroless image publishers (behind config so missing secrets skip cleanly)."
    verification: "Run `goreleaser check` and `goreleaser release --snapshot --clean`; confirm all 6 platform archives, checksums and the embedded version via running the snapshot binary's `jevkit version`. Report TODO_VERIFICATION."
    status: pending
  - id: release-workflow
    content: "Add .github/workflows/release.yml triggered on push to main after CI success: (1) compute next semver from Conventional Commits via git-cliff --bumped-version, exit cleanly if no releasable commits (chore/docs/ci/test only); (2) regenerate CHANGELOG.md and commit it back as a bot with [skip ci] using a GitHub App token or bypass rule (document the branch-protection requirement), (3) create tag vX.Y.Z, (4) run GoReleaser to publish the GitHub Release, packages, signatures and attestations. Must be idempotent and safe on re-run (skip if tag exists) and use least-privilege permissions."
    verification: "Dry-run the workflow logic locally with `act` or a scratch repo: feat commit -> minor release with CHANGELOG commit + tag + Release; docs-only commit -> no release; re-run -> no duplicate. Report TODO_VERIFICATION."
    status: pending

  # ---- Phase 6: distribution channels ----
  - id: install-script
    content: "Write install.sh (POSIX sh): detect OS/arch, download the matching archive and checksums.txt from GitHub Releases (latest or JEVKIT_VERSION), verify sha256 (and cosign signature when cosign is present), install to ~/.local/bin without sudo, warn if not on PATH, and print next steps (`jevkit key set`, `jevkit install <agent>`). Add install.ps1 for Windows. Publish both as release assets."
    verification: "Run `shellcheck install.sh` and unit-test the script functions (OS/arch detection, checksum verification accepts a match and aborts on mismatch) by sourcing them with fake inputs; PowerShell script passes PSScriptAnalyzer. Report TODO_VERIFICATION: PASS or FAIL: <reason>."
    status: pending
  - id: npm-package
    content: "Create the npm distribution: root `jevkit` package with a tiny bin shim plus per-platform optional dependency packages (@jevkit/darwin-arm64 etc., esbuild pattern) populated from GoReleaser artifacts, so `npx jevkit mcp start` works in agent MCP configs."
    verification: "Run `npm pack --dry-run` for the root and platform packages and unit-test the shim resolution logic (maps platform/arch to the right optional package, errors clearly when missing). Report TODO_VERIFICATION: PASS or FAIL: <reason>."
    status: pending
  - id: self-upgrade
    content: "Implement `jevkit upgrade [--check]`: fetch the latest release metadata, verify checksum (and signature when possible), atomically replace the running binary (handle Windows file locking), and report the changelog delta. Respect installs managed by brew/npm/scoop by printing the correct upgrade command instead of self-replacing."
    verification: "Test against a local fake release server: successful upgrade, checksum failure leaves the old binary intact, and package-manager-managed detection. Run `go test ./internal/upgrade/...`. Report TODO_VERIFICATION."
    status: pending
isProject: false
---
